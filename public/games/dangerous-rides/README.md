# Dangerous Rides — Slot Machine Assets

- `backgrounds/` — cabinet background art (the full machine image, empty reel window)
- `symbols/` — individual reel icons (777, joker card, cash, chips, etc.), ideally transparent PNGs

Drop image files directly into these folders via GitHub's web UI ("Add file" → "Upload files").
